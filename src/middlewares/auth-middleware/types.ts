enum ErrorThrower {
  INVALID_TOKEN = "Invalid token",
  NOT_ALLOWED = "You are not allowed to do this.",
  INTERNAL_ERROR = "Internal server error",
  TOKEN_NOT_FOUND = "No token was found",
  TOKEN_WRONG_FORMAT = "Invalid token format",
}

export { ErrorThrower };
